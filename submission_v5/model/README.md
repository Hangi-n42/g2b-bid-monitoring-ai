# v5 로컬 정적 자산

기존 Windows 작업 폴더의 `submission_v5/model/`에서 다음 네 JSON을 이 경로에 복사합니다.

- `item_guides.json`
- `examples.json`
- `reviewed_examples.json`
- `provenance.json`

동결된 v5-F의 네 자산은 v4와 동일합니다. 원본 그대로 복사하며, 생성기를 다시 실행해 덮어쓰지 마십시오. SHA-256은 `artifacts/submit_v5_manifest.json`에 있습니다. 가공 사례는 공개 Git에 포함하지 않습니다. 모델 가중치는 이 폴더가 아닙니다.
